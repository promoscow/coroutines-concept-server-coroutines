## Coroutines Concept Load Example
### Компонент исполнения серверной логики с I/O операцией
Компонент исполняет обычную сервисную логику — принимает HTTP-запрос, и исполняет простую серверную логику:

1. 