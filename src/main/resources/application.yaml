spring:
  profiles:
    default: docker
  application:
    name: coroutines-concept-server-coroutines
server:
  port: 8011
app:
  service-prefix: server-coroutines
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  metrics:
    export:
      prometheus:
        enabled: true
    distribution:
      percentiles-histogram:
        "[http.server.requests]": true